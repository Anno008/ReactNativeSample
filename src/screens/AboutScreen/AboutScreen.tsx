import * as React from "react";
import { Text, View } from "react-native";

export const AboutScreen: React.StatelessComponent = (): JSX.Element => (
  <View>
    <Text>About screen</Text>
  </View>
);
